import { ShowIfIsNotExampleDirective } from './show-if-is-not-example.directive';

describe('ShowIfIsNotExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfIsNotExampleDirective();
    expect(directive).toBeTruthy();
  });
});
